# 1. PVC pour conserver les données de Postgres sur k3s
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-db-pvc
  namespace: goodfood-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi # Ajustez la taille selon vos besoins
---
# 2. Déploiement de PostgreSQL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-db
  namespace: goodfood-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-db
  template:
    metadata:
      labels:
        app: auth-db
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          envFrom:
            # Récupère POSTGRES_USER et POSTGRES_DB
            - configMapRef:
                name: auth-service-config
            # Récupère POSTGRES_PASSWORD
            - secretRef:
                name: auth-service-secret
          volumeMounts:
            - name: auth-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: auth-db-data
          persistentVolumeClaim:
            claimName: auth-db-pvc
---
# 3. Service Kubernetes pour permettre à auth-service de trouver auth-db
apiVersion: v1
kind: Service
metadata:
  name: auth-db # Ce nom permet de la contacter via "host=auth-db"
  namespace: goodfood-dev
spec:
  selector:
    app: auth-db
  ports:
    - port: 5432
      targetPort: 5432
