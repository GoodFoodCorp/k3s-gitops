# 1. PVC pour conserver les données de Postgres sur k3s
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stock-db-pvc
  namespace: goodfood-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi # Ajustez la taille selon vos besoins
---
# 2. Déploiement de PostgreSQL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-db
  namespace: goodfood-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-db
  template:
    metadata:
      labels:
        app: stock-db
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          envFrom:
            # Récupère POSTGRES_USER et POSTGRES_DB
            - configMapRef:
                name: stock-service-config
            # Récupère POSTGRES_PASSWORD
            - secretRef:
                name: stock-service-secret
          volumeMounts:
            - name: stock-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: stock-db-data
          persistentVolumeClaim:
            claimName: stock-db-pvc
---
# 3. Service Kubernetes pour permettre à stock-service de trouver stock-db
apiVersion: v1
kind: Service
metadata:
  name: stock-db # Ce nom permet de la contacter via "host=stock-db"
  namespace: goodfood-dev
spec:
  selector:
    app: stock-db
  ports:
    - port: 5432
      targetPort: 5432
