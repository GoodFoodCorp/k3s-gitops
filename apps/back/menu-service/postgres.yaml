# 1. PVC pour conserver les données de Postgres sur k3s
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: menu-db-pvc
  namespace: goodfood-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi # Ajustez la taille selon vos besoins
---
# 2. Déploiement de PostgreSQL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-db
  namespace: goodfood-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: menu-db
  template:
    metadata:
      labels:
        app: menu-db
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          envFrom:
            # Récupère POSTGRES_USER et POSTGRES_DB
            - configMapRef:
                name: menu-service-config
            # Récupère POSTGRES_PASSWORD
            - secretRef:
                name: menu-service-secret
          volumeMounts:
            - name: menu-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: menu-db-data
          persistentVolumeClaim:
            claimName: menu-db-pvc
---
# 3. Service Kubernetes pour permettre à menu-service de trouver menu-db
apiVersion: v1
kind: Service
metadata:
  name: menu-db # Ce nom permet de la contacter via "host=menu-db"
  namespace: goodfood-dev
spec:
  selector:
    app: menu-db
  ports:
    - port: 5432
      targetPort: 5432
